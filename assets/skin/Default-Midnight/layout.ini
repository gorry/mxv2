; mxv2 スキン - Midnight
;
; 配色だけを変えたスキン。レイアウトと素材は Default をそのまま使う。
; Base を指定すると、そのスキンの layout.ini を読んでから
; このファイルの内容で上書きし、素材 (*.bmp / font.ttf) も
; 自分のフォルダに無ければ Base のフォルダから読む。
;
; "assets:" は同梱ぶん（このフォルダ側）を名指しする接頭辞。付けないと、
; 同じ名前のユーザースキンがあるときにそちらが土台になってしまう。

[Skin]
Base=assets:Default
