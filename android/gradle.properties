org.gradle.jvmargs=-Xmx2048m

# AndroidX は使わない（SDL の Java と自前の Activity だけで、サポート
# ライブラリに依存していない）。
android.useAndroidX=false

# ビルドする ABI。実機の確認は arm64-v8a だけで行っている。
# 増やすときは Phase H で（armeabi-v7a / x86_64）。
mxv2.abiFilters=arm64-v8a
